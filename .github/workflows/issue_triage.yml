name: Issue Triage

on:
  issues:
    types:
      - opened
      - unlabeled

jobs:
  community-issue-triage:
    uses: upbound/uptest/.github/workflows/issue-triage.yml@e68d4a22389cf0f27545621a1ee0af705c7a5847
    secrets:
      UPBOUND_BOT_GITHUB_TOKEN: ${{ secrets.UPBOUND_BOT_GITHUB_TOKEN }}
