---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: configs.identityplatform.gcp.upbound.io
spec:
  group: identityplatform.gcp.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - gcp
    kind: Config
    listKind: ConfigList
    plural: configs
    singular: config
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Config is the Schema for the Configs API. Identity Platform configuration
          for a Cloud project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConfigSpec defines the desired state of Config
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  authorizedDomains:
                    description: List of domains authorized for OAuth redirects.
                    items:
                      type: string
                    type: array
                  autodeleteAnonymousUsers:
                    description: Whether anonymous users will be auto-deleted after
                      a period of 30 days
                    type: boolean
                  blockingFunctions:
                    description: |-
                      Configuration related to blocking functions.
                      Structure is documented below.
                    properties:
                      forwardInboundCredentials:
                        description: |-
                          The user credentials to include in the JWT payload that is sent to the registered Blocking Functions.
                          Structure is documented below.
                        properties:
                          accessToken:
                            description: Whether to pass the user's OAuth identity
                              provider's access token.
                            type: boolean
                          idToken:
                            description: Whether to pass the user's OIDC identity
                              provider's ID token.
                            type: boolean
                          refreshToken:
                            description: Whether to pass the user's OAuth identity
                              provider's refresh token.
                            type: boolean
                        type: object
                      triggers:
                        description: |-
                          Map of Trigger to event type. Key should be one of the supported event types: "beforeCreate", "beforeSignIn".
                          Structure is documented below.
                        items:
                          properties:
                            eventType:
                              description: The identifier for this object. Format
                                specified above.
                              type: string
                            functionUri:
                              description: HTTP URI trigger for the Cloud Function.
                              type: string
                          type: object
                        type: array
                    type: object
                  client:
                    description: |-
                      Options related to how clients making requests on behalf of a project should be configured.
                      Structure is documented below.
                    properties:
                      permissions:
                        description: |-
                          Configuration related to restricting a user's ability to affect their account.
                          Structure is documented below.
                        properties:
                          disabledUserDeletion:
                            description: When true, end users cannot delete their
                              account on the associated project through any of our
                              API methods
                            type: boolean
                          disabledUserSignup:
                            description: When true, end users cannot sign up for a
                              new account on the associated project through any of
                              our API methods
                            type: boolean
                        type: object
                    type: object
                  mfa:
                    description: |-
                      Options related to how clients making requests on behalf of a project should be configured.
                      Structure is documented below.
                    properties:
                      enabledProviders:
                        description: |-
                          A list of usable second factors for this project.
                          Each value may be one of: PHONE_SMS.
                        items:
                          type: string
                        type: array
                      providerConfigs:
                        description: |-
                          A list of usable second factors for this project along with their configurations.
                          This field does not support phone based MFA, for that use the 'enabledProviders' field.
                          Structure is documented below.
                        items:
                          properties:
                            state:
                              description: |-
                                Whether MultiFactor Authentication has been enabled for this project.
                                Possible values are: DISABLED, ENABLED, MANDATORY.
                              type: string
                            totpProviderConfig:
                              description: |-
                                TOTP MFA provider config for this project.
                                Structure is documented below.
                              properties:
                                adjacentIntervals:
                                  description: The allowed number of adjacent intervals
                                    that will be used for verification to avoid clock
                                    skew.
                                  type: number
                              type: object
                          type: object
                        type: array
                      state:
                        description: |-
                          Whether MultiFactor Authentication has been enabled for this project.
                          Possible values are: DISABLED, ENABLED, MANDATORY.
                        type: string
                    type: object
                  monitoring:
                    description: |-
                      Configuration related to monitoring project activity.
                      Structure is documented below.
                    properties:
                      requestLogging:
                        description: |-
                          Configuration for logging requests made to this project to Stackdriver Logging
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether logging is enabled for this project
                              or not.
                            type: boolean
                        type: object
                    type: object
                  multiTenant:
                    description: |-
                      Configuration related to multi-tenant functionality.
                      Structure is documented below.
                    properties:
                      allowTenants:
                        description: Whether this project can have tenants or not.
                        type: boolean
                      defaultTenantLocation:
                        description: |-
                          The default cloud parent org or folder that the tenant project should be created under.
                          The parent resource name should be in the format of "/", such as "folders/123" or "organizations/456".
                          If the value is not set, the tenant will be created under the same organization or folder as the agent project.
                        type: string
                    type: object
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  projectRef:
                    description: Reference to a Project in cloudplatform to populate
                      project.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in cloudplatform to populate
                      project.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  quota:
                    description: |-
                      Configuration related to quotas.
                      Structure is documented below.
                    properties:
                      signUpQuotaConfig:
                        description: |-
                          Quota for the Signup endpoint, if overwritten. Signup quota is measured in sign ups per project per hour per IP. None of quota, startTime, or quotaDuration can be skipped.
                          Structure is documented below.
                        properties:
                          quota:
                            description: A sign up APIs quota that customers can override
                              temporarily. Value can be in between 1 and 1000.
                            type: number
                          quotaDuration:
                            description: 'How long this quota will be active for.
                              It is measurred in seconds, e.g., Example: "9.615s".'
                            type: string
                          startTime:
                            description: When this quota will take affect.
                            type: string
                        type: object
                    type: object
                  signIn:
                    description: |-
                      Configuration related to local sign in methods.
                      Structure is documented below.
                    properties:
                      allowDuplicateEmails:
                        description: Whether to allow more than one account to have
                          the same email.
                        type: boolean
                      anonymous:
                        description: |-
                          Configuration options related to authenticating an anonymous user.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                        type: object
                      email:
                        description: |-
                          Configuration options related to authenticating a user by their email address.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                          passwordRequired:
                            description: |-
                              Whether a password is required for email auth or not. If true, both an email and
                              password must be provided to sign in. If false, a user may sign in via either
                              email/password or email link.
                            type: boolean
                        type: object
                      phoneNumber:
                        description: |-
                          Configuration options related to authenticated a user by their phone number.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                          testPhoneNumbers:
                            additionalProperties:
                              type: string
                            description: A map of <test phone number, fake code> that
                              can be used for phone auth testing.
                            type: object
                            x-kubernetes-map-type: granular
                        type: object
                    type: object
                  smsRegionConfig:
                    description: |-
                      Configures the regions where users are allowed to send verification SMS for the project or tenant. This is based on the calling code of the destination phone number.
                      Structure is documented below.
                    properties:
                      allowByDefault:
                        description: |-
                          A policy of allowing SMS to every region by default and adding disallowed regions to a disallow list.
                          Structure is documented below.
                        properties:
                          disallowedRegions:
                            description: 'Two letter unicode region codes to disallow
                              as defined by https://cldr.unicode.org/ The full list
                              of these region codes is here: https://github.com/unicode-cldr/cldr-localenames-full/blob/master/main/en/territories.json'
                            items:
                              type: string
                            type: array
                        type: object
                      allowlistOnly:
                        description: |-
                          A policy of only allowing regions by explicitly adding them to an allowlist.
                          Structure is documented below.
                        properties:
                          allowedRegions:
                            description: 'Two letter unicode region codes to allow
                              as defined by https://cldr.unicode.org/ The full list
                              of these region codes is here: https://github.com/unicode-cldr/cldr-localenames-full/blob/master/main/en/territories.json'
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authorizedDomains:
                    description: List of domains authorized for OAuth redirects.
                    items:
                      type: string
                    type: array
                  autodeleteAnonymousUsers:
                    description: Whether anonymous users will be auto-deleted after
                      a period of 30 days
                    type: boolean
                  blockingFunctions:
                    description: |-
                      Configuration related to blocking functions.
                      Structure is documented below.
                    properties:
                      forwardInboundCredentials:
                        description: |-
                          The user credentials to include in the JWT payload that is sent to the registered Blocking Functions.
                          Structure is documented below.
                        properties:
                          accessToken:
                            description: Whether to pass the user's OAuth identity
                              provider's access token.
                            type: boolean
                          idToken:
                            description: Whether to pass the user's OIDC identity
                              provider's ID token.
                            type: boolean
                          refreshToken:
                            description: Whether to pass the user's OAuth identity
                              provider's refresh token.
                            type: boolean
                        type: object
                      triggers:
                        description: |-
                          Map of Trigger to event type. Key should be one of the supported event types: "beforeCreate", "beforeSignIn".
                          Structure is documented below.
                        items:
                          properties:
                            eventType:
                              description: The identifier for this object. Format
                                specified above.
                              type: string
                            functionUri:
                              description: HTTP URI trigger for the Cloud Function.
                              type: string
                          type: object
                        type: array
                    type: object
                  client:
                    description: |-
                      Options related to how clients making requests on behalf of a project should be configured.
                      Structure is documented below.
                    properties:
                      permissions:
                        description: |-
                          Configuration related to restricting a user's ability to affect their account.
                          Structure is documented below.
                        properties:
                          disabledUserDeletion:
                            description: When true, end users cannot delete their
                              account on the associated project through any of our
                              API methods
                            type: boolean
                          disabledUserSignup:
                            description: When true, end users cannot sign up for a
                              new account on the associated project through any of
                              our API methods
                            type: boolean
                        type: object
                    type: object
                  mfa:
                    description: |-
                      Options related to how clients making requests on behalf of a project should be configured.
                      Structure is documented below.
                    properties:
                      enabledProviders:
                        description: |-
                          A list of usable second factors for this project.
                          Each value may be one of: PHONE_SMS.
                        items:
                          type: string
                        type: array
                      providerConfigs:
                        description: |-
                          A list of usable second factors for this project along with their configurations.
                          This field does not support phone based MFA, for that use the 'enabledProviders' field.
                          Structure is documented below.
                        items:
                          properties:
                            state:
                              description: |-
                                Whether MultiFactor Authentication has been enabled for this project.
                                Possible values are: DISABLED, ENABLED, MANDATORY.
                              type: string
                            totpProviderConfig:
                              description: |-
                                TOTP MFA provider config for this project.
                                Structure is documented below.
                              properties:
                                adjacentIntervals:
                                  description: The allowed number of adjacent intervals
                                    that will be used for verification to avoid clock
                                    skew.
                                  type: number
                              type: object
                          type: object
                        type: array
                      state:
                        description: |-
                          Whether MultiFactor Authentication has been enabled for this project.
                          Possible values are: DISABLED, ENABLED, MANDATORY.
                        type: string
                    type: object
                  monitoring:
                    description: |-
                      Configuration related to monitoring project activity.
                      Structure is documented below.
                    properties:
                      requestLogging:
                        description: |-
                          Configuration for logging requests made to this project to Stackdriver Logging
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether logging is enabled for this project
                              or not.
                            type: boolean
                        type: object
                    type: object
                  multiTenant:
                    description: |-
                      Configuration related to multi-tenant functionality.
                      Structure is documented below.
                    properties:
                      allowTenants:
                        description: Whether this project can have tenants or not.
                        type: boolean
                      defaultTenantLocation:
                        description: |-
                          The default cloud parent org or folder that the tenant project should be created under.
                          The parent resource name should be in the format of "/", such as "folders/123" or "organizations/456".
                          If the value is not set, the tenant will be created under the same organization or folder as the agent project.
                        type: string
                    type: object
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  projectRef:
                    description: Reference to a Project in cloudplatform to populate
                      project.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in cloudplatform to populate
                      project.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  quota:
                    description: |-
                      Configuration related to quotas.
                      Structure is documented below.
                    properties:
                      signUpQuotaConfig:
                        description: |-
                          Quota for the Signup endpoint, if overwritten. Signup quota is measured in sign ups per project per hour per IP. None of quota, startTime, or quotaDuration can be skipped.
                          Structure is documented below.
                        properties:
                          quota:
                            description: A sign up APIs quota that customers can override
                              temporarily. Value can be in between 1 and 1000.
                            type: number
                          quotaDuration:
                            description: 'How long this quota will be active for.
                              It is measurred in seconds, e.g., Example: "9.615s".'
                            type: string
                          startTime:
                            description: When this quota will take affect.
                            type: string
                        type: object
                    type: object
                  signIn:
                    description: |-
                      Configuration related to local sign in methods.
                      Structure is documented below.
                    properties:
                      allowDuplicateEmails:
                        description: Whether to allow more than one account to have
                          the same email.
                        type: boolean
                      anonymous:
                        description: |-
                          Configuration options related to authenticating an anonymous user.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                        type: object
                      email:
                        description: |-
                          Configuration options related to authenticating a user by their email address.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                          passwordRequired:
                            description: |-
                              Whether a password is required for email auth or not. If true, both an email and
                              password must be provided to sign in. If false, a user may sign in via either
                              email/password or email link.
                            type: boolean
                        type: object
                      phoneNumber:
                        description: |-
                          Configuration options related to authenticated a user by their phone number.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                          testPhoneNumbers:
                            additionalProperties:
                              type: string
                            description: A map of <test phone number, fake code> that
                              can be used for phone auth testing.
                            type: object
                            x-kubernetes-map-type: granular
                        type: object
                    type: object
                  smsRegionConfig:
                    description: |-
                      Configures the regions where users are allowed to send verification SMS for the project or tenant. This is based on the calling code of the destination phone number.
                      Structure is documented below.
                    properties:
                      allowByDefault:
                        description: |-
                          A policy of allowing SMS to every region by default and adding disallowed regions to a disallow list.
                          Structure is documented below.
                        properties:
                          disallowedRegions:
                            description: 'Two letter unicode region codes to disallow
                              as defined by https://cldr.unicode.org/ The full list
                              of these region codes is here: https://github.com/unicode-cldr/cldr-localenames-full/blob/master/main/en/territories.json'
                            items:
                              type: string
                            type: array
                        type: object
                      allowlistOnly:
                        description: |-
                          A policy of only allowing regions by explicitly adding them to an allowlist.
                          Structure is documented below.
                        properties:
                          allowedRegions:
                            description: 'Two letter unicode region codes to allow
                              as defined by https://cldr.unicode.org/ The full list
                              of these region codes is here: https://github.com/unicode-cldr/cldr-localenames-full/blob/master/main/en/territories.json'
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ConfigStatus defines the observed state of Config.
            properties:
              atProvider:
                properties:
                  authorizedDomains:
                    description: List of domains authorized for OAuth redirects.
                    items:
                      type: string
                    type: array
                  autodeleteAnonymousUsers:
                    description: Whether anonymous users will be auto-deleted after
                      a period of 30 days
                    type: boolean
                  blockingFunctions:
                    description: |-
                      Configuration related to blocking functions.
                      Structure is documented below.
                    properties:
                      forwardInboundCredentials:
                        description: |-
                          The user credentials to include in the JWT payload that is sent to the registered Blocking Functions.
                          Structure is documented below.
                        properties:
                          accessToken:
                            description: Whether to pass the user's OAuth identity
                              provider's access token.
                            type: boolean
                          idToken:
                            description: Whether to pass the user's OIDC identity
                              provider's ID token.
                            type: boolean
                          refreshToken:
                            description: Whether to pass the user's OAuth identity
                              provider's refresh token.
                            type: boolean
                        type: object
                      triggers:
                        description: |-
                          Map of Trigger to event type. Key should be one of the supported event types: "beforeCreate", "beforeSignIn".
                          Structure is documented below.
                        items:
                          properties:
                            eventType:
                              description: The identifier for this object. Format
                                specified above.
                              type: string
                            functionUri:
                              description: HTTP URI trigger for the Cloud Function.
                              type: string
                            updateTime:
                              description: |-
                                (Output)
                                When the trigger was changed.
                              type: string
                          type: object
                        type: array
                    type: object
                  client:
                    description: |-
                      Options related to how clients making requests on behalf of a project should be configured.
                      Structure is documented below.
                    properties:
                      firebaseSubdomain:
                        description: |-
                          (Output)
                          Firebase subdomain.
                        type: string
                      permissions:
                        description: |-
                          Configuration related to restricting a user's ability to affect their account.
                          Structure is documented below.
                        properties:
                          disabledUserDeletion:
                            description: When true, end users cannot delete their
                              account on the associated project through any of our
                              API methods
                            type: boolean
                          disabledUserSignup:
                            description: When true, end users cannot sign up for a
                              new account on the associated project through any of
                              our API methods
                            type: boolean
                        type: object
                    type: object
                  id:
                    description: an identifier for the resource with format projects/{{project}}/config
                    type: string
                  mfa:
                    description: |-
                      Options related to how clients making requests on behalf of a project should be configured.
                      Structure is documented below.
                    properties:
                      enabledProviders:
                        description: |-
                          A list of usable second factors for this project.
                          Each value may be one of: PHONE_SMS.
                        items:
                          type: string
                        type: array
                      providerConfigs:
                        description: |-
                          A list of usable second factors for this project along with their configurations.
                          This field does not support phone based MFA, for that use the 'enabledProviders' field.
                          Structure is documented below.
                        items:
                          properties:
                            state:
                              description: |-
                                Whether MultiFactor Authentication has been enabled for this project.
                                Possible values are: DISABLED, ENABLED, MANDATORY.
                              type: string
                            totpProviderConfig:
                              description: |-
                                TOTP MFA provider config for this project.
                                Structure is documented below.
                              properties:
                                adjacentIntervals:
                                  description: The allowed number of adjacent intervals
                                    that will be used for verification to avoid clock
                                    skew.
                                  type: number
                              type: object
                          type: object
                        type: array
                      state:
                        description: |-
                          Whether MultiFactor Authentication has been enabled for this project.
                          Possible values are: DISABLED, ENABLED, MANDATORY.
                        type: string
                    type: object
                  monitoring:
                    description: |-
                      Configuration related to monitoring project activity.
                      Structure is documented below.
                    properties:
                      requestLogging:
                        description: |-
                          Configuration for logging requests made to this project to Stackdriver Logging
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether logging is enabled for this project
                              or not.
                            type: boolean
                        type: object
                    type: object
                  multiTenant:
                    description: |-
                      Configuration related to multi-tenant functionality.
                      Structure is documented below.
                    properties:
                      allowTenants:
                        description: Whether this project can have tenants or not.
                        type: boolean
                      defaultTenantLocation:
                        description: |-
                          The default cloud parent org or folder that the tenant project should be created under.
                          The parent resource name should be in the format of "/", such as "folders/123" or "organizations/456".
                          If the value is not set, the tenant will be created under the same organization or folder as the agent project.
                        type: string
                    type: object
                  name:
                    description: The name of the Config resource
                    type: string
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  quota:
                    description: |-
                      Configuration related to quotas.
                      Structure is documented below.
                    properties:
                      signUpQuotaConfig:
                        description: |-
                          Quota for the Signup endpoint, if overwritten. Signup quota is measured in sign ups per project per hour per IP. None of quota, startTime, or quotaDuration can be skipped.
                          Structure is documented below.
                        properties:
                          quota:
                            description: A sign up APIs quota that customers can override
                              temporarily. Value can be in between 1 and 1000.
                            type: number
                          quotaDuration:
                            description: 'How long this quota will be active for.
                              It is measurred in seconds, e.g., Example: "9.615s".'
                            type: string
                          startTime:
                            description: When this quota will take affect.
                            type: string
                        type: object
                    type: object
                  signIn:
                    description: |-
                      Configuration related to local sign in methods.
                      Structure is documented below.
                    properties:
                      allowDuplicateEmails:
                        description: Whether to allow more than one account to have
                          the same email.
                        type: boolean
                      anonymous:
                        description: |-
                          Configuration options related to authenticating an anonymous user.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                        type: object
                      email:
                        description: |-
                          Configuration options related to authenticating a user by their email address.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                          passwordRequired:
                            description: |-
                              Whether a password is required for email auth or not. If true, both an email and
                              password must be provided to sign in. If false, a user may sign in via either
                              email/password or email link.
                            type: boolean
                        type: object
                      hashConfig:
                        description: |-
                          (Output)
                          Output only. Hash config information.
                          Structure is documented below.
                        items:
                          properties:
                            algorithm:
                              description: |-
                                (Output)
                                Different password hash algorithms used in Identity Toolkit.
                              type: string
                            memoryCost:
                              description: |-
                                (Output)
                                Memory cost for hash calculation. Used by scrypt and other similar password derivation algorithms. See https://tools.ietf.org/html/rfc7914 for explanation of field.
                              type: number
                            rounds:
                              description: |-
                                (Output)
                                How many rounds for hash calculation. Used by scrypt and other similar password derivation algorithms.
                              type: number
                            saltSeparator:
                              description: |-
                                (Output)
                                Non-printable character to be inserted between the salt and plain text password in base64.
                              type: string
                            signerKey:
                              description: |-
                                (Output)
                                Signer key in base64.
                              type: string
                          type: object
                        type: array
                      phoneNumber:
                        description: |-
                          Configuration options related to authenticated a user by their phone number.
                          Structure is documented below.
                        properties:
                          enabled:
                            description: Whether phone number auth is enabled for
                              the project or not.
                            type: boolean
                          testPhoneNumbers:
                            additionalProperties:
                              type: string
                            description: A map of <test phone number, fake code> that
                              can be used for phone auth testing.
                            type: object
                            x-kubernetes-map-type: granular
                        type: object
                    type: object
                  smsRegionConfig:
                    description: |-
                      Configures the regions where users are allowed to send verification SMS for the project or tenant. This is based on the calling code of the destination phone number.
                      Structure is documented below.
                    properties:
                      allowByDefault:
                        description: |-
                          A policy of allowing SMS to every region by default and adding disallowed regions to a disallow list.
                          Structure is documented below.
                        properties:
                          disallowedRegions:
                            description: 'Two letter unicode region codes to disallow
                              as defined by https://cldr.unicode.org/ The full list
                              of these region codes is here: https://github.com/unicode-cldr/cldr-localenames-full/blob/master/main/en/territories.json'
                            items:
                              type: string
                            type: array
                        type: object
                      allowlistOnly:
                        description: |-
                          A policy of only allowing regions by explicitly adding them to an allowlist.
                          Structure is documented below.
                        properties:
                          allowedRegions:
                            description: 'Two letter unicode region codes to allow
                              as defined by https://cldr.unicode.org/ The full list
                              of these region codes is here: https://github.com/unicode-cldr/cldr-localenames-full/blob/master/main/en/territories.json'
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
