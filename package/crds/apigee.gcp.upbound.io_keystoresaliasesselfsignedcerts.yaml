---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: keystoresaliasesselfsignedcerts.apigee.gcp.upbound.io
spec:
  group: apigee.gcp.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - gcp
    kind: KeystoresAliasesSelfSignedCert
    listKind: KeystoresAliasesSelfSignedCertList
    plural: keystoresaliasesselfsignedcerts
    singular: keystoresaliasesselfsignedcert
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: KeystoresAliasesSelfSignedCert is the Schema for the KeystoresAliasesSelfSignedCerts
          API. An Environment Keystore Alias for Self Signed Certificate Format in
          Apigee
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeystoresAliasesSelfSignedCertSpec defines the desired state
              of KeystoresAliasesSelfSignedCert
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  certValidityInDays:
                    description: Validity duration of certificate, in days. Accepts
                      positive non-zero value. Defaults to 365.
                    type: number
                  environment:
                    description: The Apigee environment name
                    type: string
                  environmentRef:
                    description: Reference to a Environment in apigee to populate
                      environment.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  environmentSelector:
                    description: Selector for a Environment in apigee to populate
                      environment.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  keySize:
                    description: Key size. Default and maximum value is 2048 bits.
                    type: string
                  keystore:
                    description: The Apigee keystore name associated in an Apigee
                      environment
                    type: string
                  keystoreRef:
                    description: Reference to a EnvKeystore in apigee to populate
                      keystore.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  keystoreSelector:
                    description: Selector for a EnvKeystore in apigee to populate
                      keystore.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orgId:
                    description: The Apigee Organization name associated with the
                      Apigee environment
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in apigee to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in apigee to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  sigAlg:
                    description: Signature algorithm to generate private key. Valid
                      values are SHA512withRSA, SHA384withRSA, and SHA256withRSA
                    type: string
                  subject:
                    description: |-
                      Subject details.
                      Structure is documented below.
                    properties:
                      commonName:
                        description: Common name of the organization. Maximum length
                          is 64 characters.
                        type: string
                      countryCode:
                        description: Two-letter country code. Example, IN for India,
                          US for United States of America.
                        type: string
                      email:
                        description: Email address. Max 255 characters.
                        type: string
                      locality:
                        description: City or town name. Maximum length is 128 characters.
                        type: string
                      org:
                        description: Organization name. Maximum length is 64 characters.
                        type: string
                      orgUnit:
                        description: Organization team name. Maximum length is 64
                          characters.
                        type: string
                      state:
                        description: State or district name. Maximum length is 128
                          characters.
                        type: string
                    type: object
                  subjectAlternativeDnsNames:
                    description: |-
                      List of alternative host names. Maximum length is 255 characters for each value.
                      Structure is documented below.
                    properties:
                      subjectAlternativeName:
                        description: Subject Alternative Name
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  certValidityInDays:
                    description: Validity duration of certificate, in days. Accepts
                      positive non-zero value. Defaults to 365.
                    type: number
                  keySize:
                    description: Key size. Default and maximum value is 2048 bits.
                    type: string
                  sigAlg:
                    description: Signature algorithm to generate private key. Valid
                      values are SHA512withRSA, SHA384withRSA, and SHA256withRSA
                    type: string
                  subject:
                    description: |-
                      Subject details.
                      Structure is documented below.
                    properties:
                      commonName:
                        description: Common name of the organization. Maximum length
                          is 64 characters.
                        type: string
                      countryCode:
                        description: Two-letter country code. Example, IN for India,
                          US for United States of America.
                        type: string
                      email:
                        description: Email address. Max 255 characters.
                        type: string
                      locality:
                        description: City or town name. Maximum length is 128 characters.
                        type: string
                      org:
                        description: Organization name. Maximum length is 64 characters.
                        type: string
                      orgUnit:
                        description: Organization team name. Maximum length is 64
                          characters.
                        type: string
                      state:
                        description: State or district name. Maximum length is 128
                          characters.
                        type: string
                    type: object
                  subjectAlternativeDnsNames:
                    description: |-
                      List of alternative host names. Maximum length is 255 characters for each value.
                      Structure is documented below.
                    properties:
                      subjectAlternativeName:
                        description: Subject Alternative Name
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.sigAlg is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.sigAlg)
                || (has(self.initProvider) && has(self.initProvider.sigAlg))'
            - message: spec.forProvider.subject is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.subject)
                || (has(self.initProvider) && has(self.initProvider.subject))'
          status:
            description: KeystoresAliasesSelfSignedCertStatus defines the observed
              state of KeystoresAliasesSelfSignedCert.
            properties:
              atProvider:
                properties:
                  certValidityInDays:
                    description: Validity duration of certificate, in days. Accepts
                      positive non-zero value. Defaults to 365.
                    type: number
                  certsInfo:
                    description: |-
                      Chain of certificates under this alias.
                      Structure is documented below.
                    items:
                      properties:
                        certInfo:
                          description: |-
                            (Output)
                            List of all properties in the object.
                            Structure is documented below.
                          items:
                            properties:
                              basicConstraints:
                                description: |-
                                  (Output)
                                  X.509 basic constraints extension.
                                type: string
                              expiryDate:
                                description: |-
                                  (Output)
                                  X.509 notAfter validity period in milliseconds since epoch.
                                type: string
                              isValid:
                                description: |-
                                  (Output)
                                  Flag that specifies whether the certificate is valid.
                                  Flag is set to Yes if the certificate is valid, No if expired, or Not yet if not yet valid.
                                type: string
                              issuer:
                                description: |-
                                  (Output)
                                  X.509 issuer.
                                type: string
                              publicKey:
                                description: |-
                                  (Output)
                                  Public key component of the X.509 subject public key info.
                                type: string
                              serialNumber:
                                description: |-
                                  (Output)
                                  X.509 serial number.
                                type: string
                              sigAlgName:
                                description: |-
                                  (Output)
                                  X.509 signatureAlgorithm.
                                type: string
                              subject:
                                description: |-
                                  (Output)
                                  X.509 subject.
                                type: string
                              subjectAlternativeNames:
                                description: |-
                                  (Output)
                                  X.509 subject alternative names (SANs) extension.
                                items:
                                  type: string
                                type: array
                              validFrom:
                                description: |-
                                  (Output)
                                  X.509 notBefore validity period in milliseconds since epoch.
                                type: string
                              version:
                                description: |-
                                  (Output)
                                  X.509 version.
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  environment:
                    description: The Apigee environment name
                    type: string
                  id:
                    description: an identifier for the resource with format organizations/{{org_id}}/environments/{{environment}}/keystores/{{keystore}}/aliases/{{alias}}
                    type: string
                  keySize:
                    description: Key size. Default and maximum value is 2048 bits.
                    type: string
                  keystore:
                    description: The Apigee keystore name associated in an Apigee
                      environment
                    type: string
                  orgId:
                    description: The Apigee Organization name associated with the
                      Apigee environment
                    type: string
                  sigAlg:
                    description: Signature algorithm to generate private key. Valid
                      values are SHA512withRSA, SHA384withRSA, and SHA256withRSA
                    type: string
                  subject:
                    description: |-
                      Subject details.
                      Structure is documented below.
                    properties:
                      commonName:
                        description: Common name of the organization. Maximum length
                          is 64 characters.
                        type: string
                      countryCode:
                        description: Two-letter country code. Example, IN for India,
                          US for United States of America.
                        type: string
                      email:
                        description: Email address. Max 255 characters.
                        type: string
                      locality:
                        description: City or town name. Maximum length is 128 characters.
                        type: string
                      org:
                        description: Organization name. Maximum length is 64 characters.
                        type: string
                      orgUnit:
                        description: Organization team name. Maximum length is 64
                          characters.
                        type: string
                      state:
                        description: State or district name. Maximum length is 128
                          characters.
                        type: string
                    type: object
                  subjectAlternativeDnsNames:
                    description: |-
                      List of alternative host names. Maximum length is 255 characters for each value.
                      Structure is documented below.
                    properties:
                      subjectAlternativeName:
                        description: Subject Alternative Name
                        type: string
                    type: object
                  type:
                    description: Optional.Type of Alias
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
