---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: securityactions.apigee.gcp.m.upbound.io
spec:
  group: apigee.gcp.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - gcp
    kind: SecurityAction
    listKind: SecurityActionList
    plural: securityactions
    singular: securityaction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SecurityAction is the Schema for the SecurityActions API. A SecurityAction
          is rule that can be enforced at an environment level.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityActionSpec defines the desired state of SecurityAction
            properties:
              forProvider:
                properties:
                  allow:
                    description: Allow a request through if it matches this SecurityAction.
                    type: object
                  apiProxies:
                    description: |-
                      If unset, this would apply to all proxies in the environment.
                      If set, this action is enforced only if at least one proxy in the repeated
                      list is deployed at the time of enforcement. If set, several restrictions are enforced on SecurityActions.
                      There can be at most 100 enabled actions with proxies set in an env.
                      Several other restrictions apply on conditions and are detailed later.
                    items:
                      type: string
                    type: array
                  conditionConfig:
                    description: |-
                      A valid SecurityAction must contain at least one condition.
                      Structure is documented below.
                    properties:
                      accessTokens:
                        description: A list of accessTokens. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      apiKeys:
                        description: A list of API keys. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      apiProducts:
                        description: A list of API Products. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      asns:
                        description: |-
                          A list of ASN numbers to act on, e.g. 23. https://en.wikipedia.org/wiki/Autonomous_system_(Internet)
                          This uses int64 instead of uint32 because of https://linter.aip.dev/141/forbidden-types.
                        items:
                          type: string
                        type: array
                      botReasons:
                        description: |-
                          A list of Bot Reasons. Current options: Flooder, Brute Guessor, Static Content Scraper,
                          OAuth Abuser, Robot Abuser, TorListRule, Advanced Anomaly Detection, Advanced API Scraper,
                          Search Engine Crawlers, Public Clouds, Public Cloud AWS, Public Cloud Azure, and Public Cloud Google.
                        items:
                          type: string
                        type: array
                      developerApps:
                        description: A list of developer apps. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      developers:
                        description: A list of developers. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      httpMethods:
                        description: |-
                          Act only on particular HTTP methods. E.g. A read-only API can block POST/PUT/DELETE methods.
                          Accepted values are: GET, HEAD, POST, PUT, DELETE, CONNECT, OPTIONS, TRACE and PATCH.
                        items:
                          type: string
                        type: array
                      ipAddressRanges:
                        description: A list of IP addresses. This could be either
                          IPv4 or IPv6. Limited to 100 per action.
                        items:
                          type: string
                        type: array
                      regionCodes:
                        description: A list of countries/region codes to act on, e.g.
                          US. This follows https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2.
                        items:
                          type: string
                        type: array
                      userAgents:
                        description: A list of user agents to deny. We look for exact
                          matches. Limit 50 per action.
                        items:
                          type: string
                        type: array
                    type: object
                  deny:
                    description: |-
                      Deny a request through if it matches this SecurityAction.
                      Structure is documented below.
                    properties:
                      responseCode:
                        description: The HTTP response code if the Action = DENY.
                        type: number
                    type: object
                  description:
                    description: An optional user provided description of the SecurityAction.
                    type: string
                  envId:
                    description: The Apigee environment that this security action
                      applies to.
                    type: string
                  envIdRef:
                    description: Reference to a Environment in apigee to populate
                      envId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  envIdSelector:
                    description: Selector for a Environment in apigee to populate
                      envId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  expireTime:
                    description: |-
                      The expiration for this SecurityAction.
                      Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9
                      fractional digits. Offsets other than "Z" are also accepted.
                      Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30".
                    type: string
                  flag:
                    description: |-
                      Flag a request through if it matches this SecurityAction.
                      Structure is documented below.
                    properties:
                      headers:
                        description: |-
                          A list of HTTP headers to be sent to the target in case of a FLAG SecurityAction.
                          Limit 5 headers per SecurityAction.
                          At least one is mandatory.
                          Structure is documented below.
                        items:
                          properties:
                            name:
                              description: The header name to be sent to the target.
                              type: string
                            value:
                              description: The header value to be sent to the target.
                              type: string
                          type: object
                        type: array
                    type: object
                  orgId:
                    description: The organization that this security action applies
                      to.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in apigee to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in apigee to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  state:
                    description: |-
                      Only an ENABLED SecurityAction is enforced. An ENABLED SecurityAction past its expiration time will not be enforced.
                      Possible values are: ENABLED, DISABLED.
                    type: string
                  ttl:
                    description: |-
                      The TTL for this SecurityAction.
                      A duration in seconds with up to nine fractional digits, ending with 's'. Example: "3.5s".
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allow:
                    description: Allow a request through if it matches this SecurityAction.
                    type: object
                  apiProxies:
                    description: |-
                      If unset, this would apply to all proxies in the environment.
                      If set, this action is enforced only if at least one proxy in the repeated
                      list is deployed at the time of enforcement. If set, several restrictions are enforced on SecurityActions.
                      There can be at most 100 enabled actions with proxies set in an env.
                      Several other restrictions apply on conditions and are detailed later.
                    items:
                      type: string
                    type: array
                  conditionConfig:
                    description: |-
                      A valid SecurityAction must contain at least one condition.
                      Structure is documented below.
                    properties:
                      accessTokens:
                        description: A list of accessTokens. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      apiKeys:
                        description: A list of API keys. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      apiProducts:
                        description: A list of API Products. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      asns:
                        description: |-
                          A list of ASN numbers to act on, e.g. 23. https://en.wikipedia.org/wiki/Autonomous_system_(Internet)
                          This uses int64 instead of uint32 because of https://linter.aip.dev/141/forbidden-types.
                        items:
                          type: string
                        type: array
                      botReasons:
                        description: |-
                          A list of Bot Reasons. Current options: Flooder, Brute Guessor, Static Content Scraper,
                          OAuth Abuser, Robot Abuser, TorListRule, Advanced Anomaly Detection, Advanced API Scraper,
                          Search Engine Crawlers, Public Clouds, Public Cloud AWS, Public Cloud Azure, and Public Cloud Google.
                        items:
                          type: string
                        type: array
                      developerApps:
                        description: A list of developer apps. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      developers:
                        description: A list of developers. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      httpMethods:
                        description: |-
                          Act only on particular HTTP methods. E.g. A read-only API can block POST/PUT/DELETE methods.
                          Accepted values are: GET, HEAD, POST, PUT, DELETE, CONNECT, OPTIONS, TRACE and PATCH.
                        items:
                          type: string
                        type: array
                      ipAddressRanges:
                        description: A list of IP addresses. This could be either
                          IPv4 or IPv6. Limited to 100 per action.
                        items:
                          type: string
                        type: array
                      regionCodes:
                        description: A list of countries/region codes to act on, e.g.
                          US. This follows https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2.
                        items:
                          type: string
                        type: array
                      userAgents:
                        description: A list of user agents to deny. We look for exact
                          matches. Limit 50 per action.
                        items:
                          type: string
                        type: array
                    type: object
                  deny:
                    description: |-
                      Deny a request through if it matches this SecurityAction.
                      Structure is documented below.
                    properties:
                      responseCode:
                        description: The HTTP response code if the Action = DENY.
                        type: number
                    type: object
                  description:
                    description: An optional user provided description of the SecurityAction.
                    type: string
                  envId:
                    description: The Apigee environment that this security action
                      applies to.
                    type: string
                  envIdRef:
                    description: Reference to a Environment in apigee to populate
                      envId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  envIdSelector:
                    description: Selector for a Environment in apigee to populate
                      envId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  expireTime:
                    description: |-
                      The expiration for this SecurityAction.
                      Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9
                      fractional digits. Offsets other than "Z" are also accepted.
                      Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30".
                    type: string
                  flag:
                    description: |-
                      Flag a request through if it matches this SecurityAction.
                      Structure is documented below.
                    properties:
                      headers:
                        description: |-
                          A list of HTTP headers to be sent to the target in case of a FLAG SecurityAction.
                          Limit 5 headers per SecurityAction.
                          At least one is mandatory.
                          Structure is documented below.
                        items:
                          properties:
                            name:
                              description: The header name to be sent to the target.
                              type: string
                            value:
                              description: The header value to be sent to the target.
                              type: string
                          type: object
                        type: array
                    type: object
                  state:
                    description: |-
                      Only an ENABLED SecurityAction is enforced. An ENABLED SecurityAction past its expiration time will not be enforced.
                      Possible values are: ENABLED, DISABLED.
                    type: string
                  ttl:
                    description: |-
                      The TTL for this SecurityAction.
                      A duration in seconds with up to nine fractional digits, ending with 's'. Example: "3.5s".
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.conditionConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.conditionConfig)
                || (has(self.initProvider) && has(self.initProvider.conditionConfig))'
            - message: spec.forProvider.state is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.state)
                || (has(self.initProvider) && has(self.initProvider.state))'
          status:
            description: SecurityActionStatus defines the observed state of SecurityAction.
            properties:
              atProvider:
                properties:
                  allow:
                    description: Allow a request through if it matches this SecurityAction.
                    type: object
                  apiProxies:
                    description: |-
                      If unset, this would apply to all proxies in the environment.
                      If set, this action is enforced only if at least one proxy in the repeated
                      list is deployed at the time of enforcement. If set, several restrictions are enforced on SecurityActions.
                      There can be at most 100 enabled actions with proxies set in an env.
                      Several other restrictions apply on conditions and are detailed later.
                    items:
                      type: string
                    type: array
                  conditionConfig:
                    description: |-
                      A valid SecurityAction must contain at least one condition.
                      Structure is documented below.
                    properties:
                      accessTokens:
                        description: A list of accessTokens. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      apiKeys:
                        description: A list of API keys. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      apiProducts:
                        description: A list of API Products. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      asns:
                        description: |-
                          A list of ASN numbers to act on, e.g. 23. https://en.wikipedia.org/wiki/Autonomous_system_(Internet)
                          This uses int64 instead of uint32 because of https://linter.aip.dev/141/forbidden-types.
                        items:
                          type: string
                        type: array
                      botReasons:
                        description: |-
                          A list of Bot Reasons. Current options: Flooder, Brute Guessor, Static Content Scraper,
                          OAuth Abuser, Robot Abuser, TorListRule, Advanced Anomaly Detection, Advanced API Scraper,
                          Search Engine Crawlers, Public Clouds, Public Cloud AWS, Public Cloud Azure, and Public Cloud Google.
                        items:
                          type: string
                        type: array
                      developerApps:
                        description: A list of developer apps. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      developers:
                        description: A list of developers. Limit 1000 per action.
                        items:
                          type: string
                        type: array
                      httpMethods:
                        description: |-
                          Act only on particular HTTP methods. E.g. A read-only API can block POST/PUT/DELETE methods.
                          Accepted values are: GET, HEAD, POST, PUT, DELETE, CONNECT, OPTIONS, TRACE and PATCH.
                        items:
                          type: string
                        type: array
                      ipAddressRanges:
                        description: A list of IP addresses. This could be either
                          IPv4 or IPv6. Limited to 100 per action.
                        items:
                          type: string
                        type: array
                      regionCodes:
                        description: A list of countries/region codes to act on, e.g.
                          US. This follows https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2.
                        items:
                          type: string
                        type: array
                      userAgents:
                        description: A list of user agents to deny. We look for exact
                          matches. Limit 50 per action.
                        items:
                          type: string
                        type: array
                    type: object
                  createTime:
                    description: |-
                      The create time for this SecurityAction.
                      Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9 fractional digits.
                      Offsets other than "Z" are also accepted. Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30".
                    type: string
                  deny:
                    description: |-
                      Deny a request through if it matches this SecurityAction.
                      Structure is documented below.
                    properties:
                      responseCode:
                        description: The HTTP response code if the Action = DENY.
                        type: number
                    type: object
                  description:
                    description: An optional user provided description of the SecurityAction.
                    type: string
                  envId:
                    description: The Apigee environment that this security action
                      applies to.
                    type: string
                  expireTime:
                    description: |-
                      The expiration for this SecurityAction.
                      Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9
                      fractional digits. Offsets other than "Z" are also accepted.
                      Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30".
                    type: string
                  flag:
                    description: |-
                      Flag a request through if it matches this SecurityAction.
                      Structure is documented below.
                    properties:
                      headers:
                        description: |-
                          A list of HTTP headers to be sent to the target in case of a FLAG SecurityAction.
                          Limit 5 headers per SecurityAction.
                          At least one is mandatory.
                          Structure is documented below.
                        items:
                          properties:
                            name:
                              description: The header name to be sent to the target.
                              type: string
                            value:
                              description: The header value to be sent to the target.
                              type: string
                          type: object
                        type: array
                    type: object
                  id:
                    description: an identifier for the resource with format organizations/{{org_id}}/environments/{{env_id}}/securityActions/{{security_action_id}}
                    type: string
                  orgId:
                    description: The organization that this security action applies
                      to.
                    type: string
                  state:
                    description: |-
                      Only an ENABLED SecurityAction is enforced. An ENABLED SecurityAction past its expiration time will not be enforced.
                      Possible values are: ENABLED, DISABLED.
                    type: string
                  ttl:
                    description: |-
                      The TTL for this SecurityAction.
                      A duration in seconds with up to nine fractional digits, ending with 's'. Example: "3.5s".
                    type: string
                  updateTime:
                    description: |-
                      The update time for this SecurityAction. This reflects when this SecurityAction changed states.
                      Uses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9 fractional digits.
                      Offsets other than "Z" are also accepted. Examples: "2014-10-02T15:01:23Z", "2014-10-02T15:01:23.045123456Z" or "2014-10-02T15:01:23+05:30".
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
