apiVersion: oslogin.gcp.upbound.io/v1alpha1
kind: SSHPublicKey
metadata:
  name: example
spec:
  forProvider:
    key: ${file("path/to/id_rsa.pub")}
    user: ${data.google_client_openid_userinfo.me.email}
  providerConfigRef:
    name: example
