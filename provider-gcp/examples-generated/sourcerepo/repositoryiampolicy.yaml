apiVersion: sourcerepo.gcp.upbound.io/v1alpha1
kind: RepositoryIAMPolicy
metadata:
  name: example
spec:
  forProvider:
    policyData: ${data.google_iam_policy.admin.policy_data}
    project: ${google_sourcerepo_repository.my-repo.project}
    repository: my/repository
  providerConfigRef:
    name: example
