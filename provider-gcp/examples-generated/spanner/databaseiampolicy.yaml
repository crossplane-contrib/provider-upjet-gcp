apiVersion: spanner.gcp.upbound.io/v1alpha1
kind: DatabaseIAMPolicy
metadata:
  name: example
spec:
  forProvider:
    database: your-database-name
    instance: your-instance-name
    policyData: ${data.google_iam_policy.admin.policy_data}
  providerConfigRef:
    name: example
