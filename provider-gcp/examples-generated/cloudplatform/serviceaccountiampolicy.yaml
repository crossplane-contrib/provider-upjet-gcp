apiVersion: cloudplatform.gcp.upbound.io/v1alpha1
kind: ServiceAccountIAMPolicy
metadata:
  name: example
spec:
  forProvider:
    policyData: ${data.google_iam_policy.admin.policy_data}
    serviceAccountId: ${google_service_account.sa.name}
  providerConfigRef:
    name: example
