apiVersion: appengine.gcp.upbound.io/v1alpha1
kind: DomainMapping
metadata:
  name: example
spec:
  forProvider:
    domainName: verified-domain.com
    sslSettings:
    - sslManagementType: AUTOMATIC
  providerConfigRef:
    name: example
