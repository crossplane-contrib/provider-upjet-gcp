apiVersion: compute.gcp.upbound.io/v1alpha1
kind: BackendBucket
metadata:
  name: example
spec:
  forProvider:
    bucketName: ${google_storage_bucket.image_bucket.name}
    description: Contains beautiful images
    enableCdn: true
    name: image-backend-bucket
  providerConfigRef:
    name: example
